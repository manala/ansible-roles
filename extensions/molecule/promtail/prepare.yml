---

- name: Prepare
  tags: [always]
  hosts:
    - debian
    - "!debian.bullseye"
  tasks:
    - name: Packages
      ansible.builtin.apt:
        name: [unzip]
        install_recommends: false
        update_cache: true
        cache_valid_time: 3600
