---

- name: "{{ test }}"
  hosts: all
  vars:
    manala_mysql_users:
      - name:     admin
        password: "bTxKGlBi"
        host:     "localhost"
        priv:     "*.*:ALL,GRANT"
  roles:
    - manala.mysql
  pre_tasks:
    - include: pre_tasks/mysql_56.yml
  post_tasks:
    - name: Goss
      command: goss --gossfile {{ test }}.goss.yml validate
