---

package:
  mariadb-server:
    installed: true

package:
  mariadb-client:
    installed: true

command:
  mysql --version:
    exit-status: 0
    stdout:
      - "/mysql(.)*Distrib 10.0.29-MariaDB/"
