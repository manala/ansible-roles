---

- name: install > Packages dependencies
  apt:
    name:  libfontconfig1
    state: present

- name: install > Packages
  npm:
    name:    "{{ item }}"
    state:   present
    global:  true
  with_items:
    - phantomjs


