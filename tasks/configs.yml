---

- name: configs > Templates
  template:
    src:  "{{ item.template|default('configs/default.j2') }}"
    dest: "{{ elao_logrotate_configs_path }}/{{ item.name }}"
  with_items: elao_logrotate_configs
