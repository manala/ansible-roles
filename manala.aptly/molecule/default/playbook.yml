---

- name: Converge
  hosts: all
  vars:
    manala_aptly_config:
      - rootDir: /tmp/aptly
      - architectures:
          - amd64
    manala_aptly_repositories:
      - name:         foo
        comment:      Foo
        component:    main
        distribution: jessie
        origin:       Bar
        label:        Bar
  roles:
    - manala.aptly
