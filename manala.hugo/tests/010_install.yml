---

- name: "{{ test }}"
  hosts: all
  become: true
  pre_tasks:
    - include: pre_tasks/manala.yml
  roles:
    - manala.hugo
  post_tasks:
    - name: Goss
      command: goss --gossfile {{ test }}.goss.yml validate
