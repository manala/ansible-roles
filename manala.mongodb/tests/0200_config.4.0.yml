---

- name: "{{ test }}"
  hosts: debian
  become: true
  vars:
    # See: https://github.com/manala/ansible-roles/issues/115
    manala_mongodb:
      services: false
    manala_mongodb_version: 4.0
    manala_mongodb_config_template: config/default.prod.j2
    manala_mongodb_config:
      - security:
        - authorization: enabled
  pre_tasks:
    - import_tasks: pre_tasks/mongodb_4_0.yml
    - copy:
        dest: /etc/apt/preferences.d/mongodb
        content: |
          Package:      mongodb*
          Pin:          origin docs.mongodb.org
          Pin-Priority: 900
  roles:
    - manala.mongodb
  post_tasks:
    - name: Goss
      command: goss --gossfile {{ test }}.goss.yml validate
