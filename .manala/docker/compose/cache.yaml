services:

    ##########
    # System #
    ##########

    system:
        build:
            cache_from:
              - ${MANALA_DOCKER_CACHE_FROM}
            cache_to:
              - ${MANALA_DOCKER_CACHE_TO}
