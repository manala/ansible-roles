---

- name: Prepare
  hosts: all
  tasks:
    - copy:
        dest: /etc/environment
        content: |
          _FOO="bar"
          _BAR="123"
          BAZ="baz"
          FOO="foo"
    - file:
        path: /etc/zsh
        state: directory
    - copy:
        dest: /etc/zsh/zshenv
        content: |
          # /etc/zsh/zshenv
    - copy:
        dest: /etc/profile.d/test.sh
        content: |
          # /etc/profile.d/test.sh
  roles:
    - manala.environment
