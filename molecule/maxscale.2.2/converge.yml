---

###########
# Default #
###########

- name: Default
  tags: [default]
  hosts:
    - debian.stretch
  tasks:
    - block:
        - name: Role
          ansible.builtin.import_role:
            name: manala.roles.maxscale
