name: Molecule - MySQL

on:
  pull_request:
    paths:
      - .manala.yaml
      - .github/workflows/molecule.mysql.yaml
      - extensions/molecule/config.yml
      - extensions/molecule/Dockerfile.*
      - extensions/molecule/mysql.*/**
      - roles/mysql/**
      # Plugins
      - plugins/filter/mysql_config.py
      - plugins/filter/staten.py
      - plugins/lookup/templates_exclusive.py
  workflow_dispatch:
    inputs:
      debug:
        type: boolean
        description: Run with tmate debugging
        required: false
        default: false

jobs:
  mysql:
    uses: ./.github/workflows/molecule.yaml
    with:
      debug: ${{ inputs.debug == true }}
      scenario: mysql
      versions: '["8.0", "8.4", "galera.4.10.mysql_wsrep.8.0.26", "mariadb.10.5", "mariadb.10.6", "mariadb.10.11", "mariadb.11.4", "mariadb.11.8"]'
