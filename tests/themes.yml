---

- hosts: localhost

  vars:

    manala_ohmyzsh_users:
      - user:     root
        group:    root
        template: users/php.dev.j2

  pre_tasks:
    - apt:
        package: git
    - apt:
        package: zsh

  roles:
    - manala.ohmyzsh
