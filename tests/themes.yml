---

- hosts: localhost

  vars:

    manala_ohmyzsh_users:
      - user:     root
        group:    root
        template: users/php.dev.j2

  pre_tasks:
    - apt:
        package: "{{ item }}"
      with_items:
        - git
        - zsh

  roles:
    - manala.ohmyzsh
