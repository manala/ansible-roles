---

- name: tasks/make
  tags:
    - manala_deploy
  block:

    - name: tasks/make > "{{ item.options }}" in "{{ item.dir }}"
      ansible.builtin.command: "make {{ item.options }}"
      args:
        chdir: "{{ item.dir }}"
      changed_when: true
