---

# Default
{{ if has "default" .Vars.tags }}
file:
  /tmp/foo:
    exists: true
    filetype: file
    contains:
      - Hello world!
  /tmp/bar:
    exists: true
    filetype: file
    contains:
      - Hello world!
  /tmp/baz:
    exists: true
    filetype: file
    contains:
      - Hello world!
{{ end }}

# Absent
{{ if has "absent" .Vars.tags }}
file:
  /tmp/qux:
    exists: false
{{ end }}
