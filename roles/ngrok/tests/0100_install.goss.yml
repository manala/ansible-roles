---

# Latest
{{ if has "latest" .Vars.tags }}
file:
  /usr/local/bin/ngrok:
    exists: true
    mode: "0755"
    owner: root
    filetype: file
command:
  ngrok --version:
    exit-status: 0
    stdout:
      - ngrok version
      - "!ngrok version 2.2.2"
{{ end }}

# Version
{{ if has "version" .Vars.tags }}
file:
  /usr/local/bin/ngrok:
    exists: true
    mode: "0755"
    owner: root
    filetype: file
command:
  ngrok --version:
    exit-status: 0
    stdout:
      - ngrok version 2.2.2
{{ end }}
