---

file:
  /etc/nginx/conf.d/foo.conf:
    exists: true
    mode: "0644"
    owner: root
    group: root
    filetype: file
    contains:
      - server {
      - listen 8080;
      - location / {
      - root /srv/foo;
