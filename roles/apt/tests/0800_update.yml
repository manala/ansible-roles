---

- name: "{{ test }}"
  hosts: debian
  become: true
  tasks:

    - block:
        - import_role:
            name: manala.apt
          vars:
            manala_apt:
              update: true
            manala_apt_update_holds:
              - foo
              - unzip
