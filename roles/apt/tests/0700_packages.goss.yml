---

{{ if has "default" .Vars.tags }}
package:
  asciijump:
    installed: true
  freesweep:
    installed: true
  an:
    installed: false
  spinner:
    installed: true
    versions:
      - 1.2.4-3
  wait-for-it:
    installed: true
  pscan:
    installed: false
{{ end }}

# State
{{ if has "state" .Vars.tags }}
package:
  asciijump:
    installed: true
  an:
    installed: false
  freesweep:
    installed: true
  nettoe:
    installed: true
  wordplay:
    installed: false
{{ end }}
