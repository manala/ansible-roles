{%- set config = manala_git_config -%}

{%- if config is mapping -%}

{{ config | manala_git_config }}

{%- elif config is iterable and config is not string -%}

{#- Deprecated -#}
{%- import '_macros.j2' as macros with context -%}

{{ macros.config(config) }}

{%- else -%}

{{ config }}

{%- endif -%}
