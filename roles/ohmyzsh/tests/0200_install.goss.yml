---

file:
  tmp/install:
    exists: true
    filetype: directory
    owner: root
    group: root
    mode: "0755"

command:
  git --git-dir=tmp/install/.git --work-tree=tmp/install status:
    exit-status: 0
    stdout:
      - On branch master
