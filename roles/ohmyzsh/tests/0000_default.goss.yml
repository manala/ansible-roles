---

file:
  /usr/local/share/oh-my-zsh:
    exists: true
    filetype: directory
    owner: root
    group: root
    mode: "0755"

command:
  git --git-dir=/usr/local/share/oh-my-zsh/.git --work-tree=/usr/local/share/oh-my-zsh status:
    exit-status: 0
    stdout:
      - On branch master
