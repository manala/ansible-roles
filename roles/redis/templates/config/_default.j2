{%- set config = manala_redis_server_config -%}

{%- if config is mapping -%}

{{ config | manala_redis_config }}

{%- elif config is iterable and config is not string -%}

{#- Deprecated -#}
{%- set template_env = '' -%}
{%- include '_base.j2' -%}

{%- else -%}

{{ config }}

{%- endif -%}
