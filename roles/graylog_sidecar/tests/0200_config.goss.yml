---

file:
  /etc/graylog/sidecar/sidecar.yml:
    exists: true
    filetype: file
    owner: root
    group: root
    mode: "0644"
    contains:
      - "http://foo.bar:9000/api"
      - "foo-bar-baz-bar-foo"
      - "foo.bar.baz.hostname"
