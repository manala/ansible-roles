---

package:
  sudo:
    installed: true

file:
  /etc/sudoers.d.test/vagrant:
    exists: true
    mode: "0440"
    owner: root
    group: root
    filetype: file
    contains:
      - vagrant ALL=NOPASSWD:ALL

  /etc/sudoers.d.test/README:
    exists: true

  /etc/sudoers.d.test/foo:
    exists: true

  /etc/sudoers.d.test/bar:
    exists: true

  /etc/sudoers.d.test/baz:
    exists: true

  /etc/sudoers.d.test/exclusive:
    exists: false
