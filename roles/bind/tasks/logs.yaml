---

- name: Logs > Directory
  ansible.builtin.file:
    path: "{{ manala_bind_log_dir }}"
    state: directory
    owner: "{{ manala_bind_user }}"
    group: "{{ manala_bind_group }}"
    mode: "0755"
