---

- name: "{{ test }}"
  hosts: debian
  become: true
  vars:
    manala_symfony_cli_version: 4.16.0
  roles:
    - symfony_cli
  post_tasks:
    - name: Goss
      command: goss --gossfile {{ test }}.goss.yml validate
