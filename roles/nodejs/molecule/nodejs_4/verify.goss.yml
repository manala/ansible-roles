---

package:
  nodejs:
    installed: true

command:
  node --version:
    exit-status: 0
    stdout:
      - "/^v4.\\d+\\.\\d+$/"
  npm --version:
    exit-status: 0
    stdout:
      - "/^2.\\d+\\.\\d+$/"
