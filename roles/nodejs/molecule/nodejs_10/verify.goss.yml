---

package:
  nodejs:
    installed: true

command:
  node --version:
    exit-status: 0
    stdout:
      - "/^v10.\\d+\\.\\d+$/"
  npm --version:
    exit-status: 0
    stdout:
      - "/^6.\\d+\\.\\d+$/"
