---

package:
  nodejs:
    installed: true

command:
  node --version:
    exit-status: 0
    stdout:
      - "/^v0.12.\\d+$/"
  npm --version:
    exit-status: 0
    stdout:
      - "/^2.\\d+\\.\\d+$/"
