---

package:
  nodejs:
    installed: true

command:
  node --version:
    exit-status: 0
    stdout:
      - "/^v5.\\d+\\.\\d+$/"
  npm --version:
    exit-status: 0
    stdout:
      - "/^3.\\d+\\.\\d+$/"
