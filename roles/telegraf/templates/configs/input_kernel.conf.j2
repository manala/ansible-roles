{#- Deprecated -#}

{%- import '_macros.j2' as macros with context -%}

{% set config = item.config|default([]) -%}
{% set section = 'inputs.kernel' -%}

# Get kernel statistics from /proc/stat
[[{{ section }}]]
  {%- if not config %}{{ '\n' }}  # no configuration{% endif %}

{{ macros.config(config, [
  'tags',
  'tagpass',
  'tagdrop'
], 2) }}

{%- include '_tags.j2' %}
