---

- name: "{{ test }}"
  hosts: debian
  become: true
  vars:
    manala_gomplate_version: 3.7.0
  roles:
    - gomplate
  post_tasks:
    - name: Goss
      command: goss --gossfile {{ test }}.goss.yml validate
